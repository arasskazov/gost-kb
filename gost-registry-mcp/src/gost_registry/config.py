"""Конфигурация приложения: пути по умолчанию и константы.

Все пути можно переопределить через аргументы командной строки
importer.py / mcp_server.py — значения здесь являются только
разумными значениями по умолчанию для локального запуска.
"""
from __future__ import annotations

from pathlib import Path

# Корень проекта (родитель каталога src/)
PROJECT_ROOT = Path(__file__).resolve().parents[2]

DEFAULT_DB_PATH = PROJECT_ROOT / "data" / "gost_registry.sqlite"
DEFAULT_INCOMING_DIR = PROJECT_ROOT / "data" / "incoming"
DEFAULT_REPORTS_DIR = PROJECT_ROOT / "data" / "reports"

# Известные серии стандартов первой версии (раздел 2.2 ТЗ)
KNOWN_SERIES = ("ГОСТ ISO", "ГОСТ ИСО", "ГОСТ Р", "ГОСТ", "ПНСТ")

# Ограничение достоверности, выдаваемое во всех ответах (раздел 6.6 ТЗ)
PROVENANCE_DISCLAIMER = (
    "Результат основан на локальном импортированном снимке и не подтверждает "
    "юридическую актуальность вне этого снимка."
)

NEGATIVE_RESULT_DISCLAIMER = (
    "Проверка выполнена по локальному импортированному снимку реестра; "
    "результат не подтверждает отсутствие стандарта в иных источниках и "
    "не определяет его юридическую применимость."
)

# Обязательные исходные поля JSON-записи (раздел 2.1 ТЗ). Обратите внимание
# на завершающий пробел в имени поля заглавия — это особенность исходной
# схемы выгрузок, а не опечатка.
FIELD_DESIGNATION = "Обозначение"
FIELD_TITLE = "Заглавие на русском языке "
FIELD_STATUS = "Статус"
FIELD_ICS = "Код ОКС"
